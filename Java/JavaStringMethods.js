/*
    string methods
    charAt
    concat
    startswith
    endswith
    indexOf
    lastIndexOf
    length
    replace
    replaceAll
    tolowercase
    touppercase
    trim
    contains

    charAt in java
    we have to take the word as a string then scan.next.charAt then
    store it in one variable.

    **Compare 2 strings in Java

    take 2 inputs, then  
    name.equals(othername) case sensitive
    name.equalsIgnoreCase(othername)

    **name.isEmpty(); if the user entered nothing, space bar adds white spaces
*/